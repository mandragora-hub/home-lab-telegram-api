import { composer as utils } from "./utils";
import { composer as register } from "./register";
import { composer as setting } from "./setting";
import { composer as gemini } from "./gemini";

export default { utils, register, setting, gemini };
